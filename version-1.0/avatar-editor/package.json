{
  "name": "habbo-registration-v1",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "extract": "node scripts/extract-assets.ts",
    "optimize": "node scripts/optimize-assets.ts",
    "extract-and-optimize": "npm run extract && npm run optimize",
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.4.0",
    "vite": "^5.4.0"
  }
}
